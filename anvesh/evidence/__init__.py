"""Evidence package boundary.

M3 implements only reliability scoring (`reliability.py`, blueprint
Part 3 module 9) -- a per-camera, per-window heuristic quality score,
NOT cross-camera evidence fusion. Per-hypothesis evidence generation
(`evidence_builder.py`, module 8, tied to the H1-H7 cause model) is out
of scope until cause hypotheses exist -- see blueprint Part 14, milestone
M4+.
"""
